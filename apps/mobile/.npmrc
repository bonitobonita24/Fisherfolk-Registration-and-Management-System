# NOTE: pnpm + React Native needs `node-linker=hoisted` set in the REPO-ROOT .npmrc before
# `pnpm install`. That is a monorepo-wide change (affects apps/web) — validate web still builds
# after adding it. See README.md "Bring-up". Do NOT rely on this nested file for node-linker
# (pnpm reads it only from root).
