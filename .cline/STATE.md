# Project State — Fisherfolk Registration and Management System
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-17 by CLAUDE_CODE (Phase 8 Batch 2b sub-batch 1b.2 — frontend duplicate search gate shipped)

PHASE:        Phase 8 — Iterative Buildout (Batch 2b sub-batch 1b — Duplicate Search COMPLETE end-to-end; sub-batch 2b-2 — photo/signature/QR PENDING)
LAST_DONE:    Shipped Batch 2b-1b.2 (Duplicate Search — frontend gate). Created apps/web/src/app/[tenant]/fisherfolk/register/duplicate-search-client.tsx — search form mirrors backend Zod refine (idNumber OR rsbsaNumber OR firstName+lastName), uses trpc.useUtils().fisherfolk.searchForDuplicates.fetch() for imperative on-submit query, renders match cards colour-coded by matchType (red EXACT_ID/EXACT_RSBSA, amber STRONG_NAME_DOB, yellow POSSIBLE_NAME), "No matches → Proceed to register" CTA, "Match found → View existing record" link to /[tenant]/fisherfolk/[id] (placeholder for Batch 3 detail page) plus "Proceed anyway" escape hatch. Modified registration-form-client.tsx to accept optional initialValues?: Partial<FormValues> prop (purely additive, merged into useForm defaults). Modified page.tsx to mount <DuplicateSearchClient /> instead of <RegistrationFormClient /> directly. pnpm typecheck (web): clean. pnpm lint (web): 0 warnings/errors. Not yet committed — staged for human review then commit on this branch.
NEXT:         Two paths:
              (a) Commit Batch 2b-1b.2 on current branch (feat/batch-2b-1-pickers-search) → human verifies UI in dev → squash-merge entire Batch 2b-1 (pickers + duplicate search) to main.
              (b) Move directly to Batch 2b-2 (photo upload + signature upload + QR code data string) on a NEW branch in a NEW session — separate sub-batch per anti-thrashing budget. STATE.md will need re-baseline after the merge in (a).
              Recommended order: (a) first to lock in the working gate before adding upload complexity.
BLOCKERS:     none
GIT_BRANCH:   feat/batch-2b-1-pickers-search (3 commits ahead of origin: 7e14e41 pickers, 850758b governance, 30bf90e search backend, 09b858d state checkpoint). Frontend gate work staged, not yet committed.
PORTS:        APP=44387 DB=44377 CACHE=44379 MINIO=44380 PGADMIN=44384 WORKER=44388
MODELS:
  planning:   claude-code (Phase 2 — V31 primary)
  execution:  claude-sonnet-4-6 via Claude Code (V31 primary; Cline deprecated)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)

Phase 8 Batch progress:
  ✅ 1a Shared UI Components — DONE (6 components + barrel index, squash-merged to main 79e79d6)
  ✅ 1b Fisherfolk List Page  — DONE (4 files, squash-merged to main 79e79d6)
  ✅ 2a Fisherfolk Registration Form (basic) — DONE (5 files, merged 58d74fa)
  ⏳ 2b Registration polish — IN PROGRESS:
       ✅ 2b-1a Pickers (BarangayPicker + CategoryPicker + wire-in) — DONE (7e14e41)
       ✅ 2b-1b Duplicate search — DONE end-to-end:
            ✅ 2b-1b.1 Backend (Zod schema + searchForDuplicates query) — DONE (30bf90e)
            ✅ 2b-1b.2 Frontend gate UI (duplicate-search-client + register/page wiring + form initialValues prop) — DONE this session (staged, not yet committed)
       ⬜ 2b-2  Photo upload + signature upload + QR code data string — NEXT
  ⬜ 3  Fisherfolk Detail View + Vessel Registration — REMAINING
       Note: 2b-1b.2's "View existing record" link points at /[tenant]/fisherfolk/[id] which will 404 until Batch 3 ships the detail page. Acceptable — link is forward-compatible.

Outstanding follow-ups (do not block ongoing batches):
  - TDD test infra missing project-wide (no vitest/jest, zero *.test.*). Schedule "Phase 8 Batch X — test infra setup" before Rule 25 TDD can be strictly enforced. Batch 2b-1b shipped under this acknowledged debt (both halves).
  - Tenant-managed barangay list (other municipalities, auto-seed barangays by municipality, admin edit) — future Settings → Barangays feature. CALAPAN_BARANGAYS is the seed for the City of Calapan FMO tenant only. BarangayPicker is already tenant-agnostic (accepts barangays prop).
  - Calapan barangay list source: PSA Philippine Standard Geographic Code (62 barangays). Admin MUST verify spelling + completeness against current LGU records before production go-live (Rule 29 — list captured at session time, not authoritative).
  - Vercel skill packs (next-cache-components, next-forge, nextjs, react-best-practices) declined load this session AND prior — none apply to feature-level component work. Re-evaluate ONLY when doing platform-level changes (proxy.ts migration, Cache Components adoption, framework upgrade). Same precedent for Clerk auth skill — locked stack is Auth.js v5.
  - searchForDuplicates name matching is equality-based (case-insensitive). Future enhancement: fuzzy/trigram matching (pg_trgm) or weighted scoring for typo tolerance. Current impl catches exact-spelling dupes — sufficient for v1 gate. Capture user feedback during pilot before adding complexity.
  - "View existing record" link from gate match cards targets /[tenant]/fisherfolk/[id] — placeholder, resolves once Batch 3 detail page ships. Encoder workaround until then: copy the ID number, navigate to list, search.

# ─── V31 Memory Governance Fields (memory-governance.md §2) ───
TOKEN_ESTIMATE:       ~50K consumed this session (CLAUDE.md+rules load + 4 file reads + 1 Write + 3 Edits + 2 lint/typecheck cycles + governance writes). Stayed comfortably within 80K SAFE zone — under the 70K user threshold from feedback_anti_thrashing.md.
CHECKPOINT_TYPE:      full (3 source files touched in apps/web)
FILES_TOUCHED:        [
                        "apps/web/src/app/[tenant]/fisherfolk/register/duplicate-search-client.tsx",
                        "apps/web/src/app/[tenant]/fisherfolk/register/registration-form-client.tsx",
                        "apps/web/src/app/[tenant]/fisherfolk/register/page.tsx",
                        "docs/CHANGELOG_AI.md",
                        ".cline/STATE.md"
                      ]
TIER_CLASSIFICATION:  2 — moderate (3 source files, 1 module: apps/web/app/register). Single-session scope as projected.
SUB_BATCH_SPLIT:      No further split needed — 2b-1b.2 is the planned single sub-session that closes Batch 2b-1b.
