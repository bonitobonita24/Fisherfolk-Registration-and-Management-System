# Project State — Fisherfolk Registration and Management System
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-08 by CLAUDE_CODE

PHASE:        Phase 8 — Iterative Buildout (Batch 2a merged + anti-thrashing hook installed) — SESSION PAUSED
LAST_DONE:    Installed UserPromptSubmit hook in .claude/settings.json that auto-injects the anti-thrashing scope-assessment preamble when user prompt contains "Start Phase", "Continue Phase", "Feature Update", "Batch", "Resume Session", or "Resume from handoff" (case-insensitive). Single inline node -e command, preamble base64-encoded inside JS to sidestep two-layer quote escaping. 5-second timeout. 5 pipe-tests passed (match/no-match/lowercase/mid-prompt/malformed-JSON). Committed as 7bf35bf. ACTIVATION REQUIRED in current session: open /hooks once or restart Claude Code so the settings watcher picks up the new hook. Next fresh session activates automatically. Prior work (Batch 2a registration form) remains merged at 58d74fa.
NEXT:         ACTIVATE HOOK: open /hooks once in current Claude Code session, OR start fresh session. Then RESUME: open NEW session → say "Resume Session" + attach 3 docs (project.memory.md + IMPLEMENTATION_MAP.md + DECISIONS_LOG.md) → then say "Start Phase 8 Batch 2b". Hook should auto-inject scope-assessment preamble — verify by checking that Claude outputs file count + token table BEFORE any code on the next phase/batch prompt. Read .cline/handoffs/2026-05-08-pause-after-hook-install.md for full handoff + activation steps.
BLOCKERS:     none — session paused at clean checkpoint. One-time activation step required (open /hooks or restart) for hook to fire in current session; fresh sessions activate automatically.
GIT_BRANCH:   main (clean, HEAD = 7bf35bf)
PORTS:        APP=44387 DB=44377 CACHE=44379 MINIO=44380 PGADMIN=44384 WORKER=44388
MODELS:
  planning:   claude-code (Phase 2 — V31 primary)
  execution:  claude-sonnet-4-6 via Claude Code (V31 primary; Cline deprecated)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)

Phase 8 Batch progress:
  ✅ 1a Shared UI Components — DONE (6 components + barrel index, squash-merged to main 79e79d6)
  ✅ 1b Fisherfolk List Page  — DONE (4 files, squash-merged to main 79e79d6)
  ✅ 2a Fisherfolk Registration Form (basic) — DONE (5 files, squash-merge pending)
  ⬜ 2b Registration polish — REMAINING (duplicate search, photo/signature upload, QR builder, category + barangay pickers)
  ⬜ 3  Fisherfolk Detail View + Vessel Registration — REMAINING

Outstanding follow-ups (do not block Batch 2b):
  - TDD test infrastructure missing project-wide (no vitest/jest config, zero *.test.* files). Schedule a dedicated "Phase 8 Batch X — test infra setup" before Rule 25 can be enforced strictly. Tracked in lessons.md (🟤 decision dated 2026-05-08).
