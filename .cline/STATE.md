# Project State — Fisherfolk Registration and Management System
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-06-25 by CLAUDE_CODE (Governance Sync — reconciled Batch 2b-1/2b-2 changelog + map drift)

PHASE:        Phase 8 — Iterative Buildout (Batch 3a MERGED; Governance Sync done 2026-06-25; Batch 3b PENDING + AIEF alignment check requested)
LAST_DONE:    Governance Sync (2026-06-25): added CHANGELOG_AI.md reconciliation entries for Batch 2b-1 (77efa8c) + Batch 2b-2 (9ab5039); updated IMPLEMENTATION_MAP.md (Phase 8 row, Batch 2b section marked complete, decision count 12→16); cleared the resolved drift follow-up. Prior: Squash-merged Batch 3a (Fisherfolk read-only detail view + list nav) to main. Collapsed 2 feature-branch commits (12c8b7d code + 2b32d90 STATE.md update) into one squash commit on main. Pushed main to origin. Deleted feat/batch-3a-fisherfolk-detail local + remote. CHANGELOG_AI.md + IMPLEMENTATION_MAP.md updated for Batch 3a. New route /[tenant]/fisherfolk/[id] now live: server page.tsx + fisherfolk-detail-client.tsx render profile fields + photo (signed URL via upload.getDownloadUrl) + signature (same) + QR (renderQRDataUrl → PNG data URL). List page columns.tsx idNumber column wraps in <Link> via IdNumberCell sub-component (uses useParams for tenant slug). Reused existing fisherfolk.getById — no router changes. End-to-end flow now resolves: register → duplicate-search "View existing record" → detail page renders (was 404 before).
NEXT:         Open NEW session for Batch 3b — Vessel Registration. Reasoning: this session already at ~75K context (manageable but no headroom for a fresh batch). Fresh-context discipline per Rule 24. Batch 3b scope: ~7 files, ~35K estimated tokens. Reuses existing Vessel prisma declarations + pre-scaffolded vessel.ts router (needs filling in). See PRODUCT.md line 274 for Vessel entity spec, line 84-96 for module description, line 100 for QR pattern, line 274 + 261 for many-to-many FisherfolkVessel relation. Independent of Batch 3c (Edit Request workflow — separate future batch).
BLOCKERS:     none. Long-standing follow-ups carried forward.
GIT_BRANCH:   main (at the new Batch 3a squash commit, pushed to origin). Feature branch deleted local + remote.
PORTS:        APP=44387 DB=44377 CACHE=44379 MINIO=44380 PGADMIN=44384 WORKER=44388
MODELS:
  planning:   claude-code (Phase 2 — V31 primary)
  execution:  claude-sonnet-4-6 via Claude Code (V31 primary; Cline deprecated). This session ran as Opus 4.7 per environment override — flag for future cost review if pattern continues.
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)

Phase 8 Batch progress:
  ✅ 1a Shared UI Components — DONE (6 components + barrel index, squash-merged to main 79e79d6)
  ✅ 1b Fisherfolk List Page  — DONE (4 files, squash-merged to main 79e79d6)
  ✅ 2a Fisherfolk Registration Form (basic) — DONE (5 files, merged 58d74fa)
  ✅ 2b Registration polish — DONE + MERGED:
       ✅ 2b-1 Pickers + Duplicate Search — squash 77efa8c on main (28 files +2001/-191)
       ✅ 2b-2 Photo + signature + QR + Auth/L6 fix — squash on main (15 files +1065/-109, commit 9ab5039)
  ⏳ 3  Fisherfolk Detail View + Vessel Registration — IN PROGRESS:
       ✅ 3a Fisherfolk Detail View (read-only) — SQUASH-MERGED to main 2026-05-17 PM (3 files +267/-1)
       ⬜ 3b Vessel Registration — PENDING in new session (~7 files, ~35K tokens)
       ⬜ 3c Edit Request Workflow — FUTURE (split out of original Batch 3 scope; admin queue + diff viewer + notifications)

Outstanding follow-ups (do not block ongoing batches):
  - TDD test infra missing project-wide (no vitest/jest, zero *.test.*). Schedule "Phase 8 Batch X — test infra setup" before Rule 25 TDD can be strictly enforced. Batch 3a shipped under this acknowledged debt.
  - upload.getDownloadUrl is encoderProcedure. Viewer + Bantay Dagat roles cannot fetch signed URLs, so they'll see "No photo/No signature" placeholders on /[tenant]/fisherfolk/[id]. PRODUCT.md line 268 says Bantay Dagat needs identity verification via field lookup — they MUST see photos. Fix options: (a) split into two procedures (admin/encoder write + protected read); (b) make getDownloadUrl protectedProcedure with role-aware tenant scoping. Schedule as quick Batch 3d or fold into Bantay Dagat batch.
  - ✅ RESOLVED 2026-06-25 (Governance Sync): IMPLEMENTATION_MAP.md Phase row + Batch 2b section now reflect 2b-1/2b-2/3a merged; decision count corrected 12→16; CHANGELOG_AI.md gained reconciliation entries for Batch 2b-1 (77efa8c) and Batch 2b-2 (9ab5039).
  - Tenant-managed barangay list (other municipalities, auto-seed barangays by municipality, admin edit) — future Settings → Barangays feature.
  - Calapan barangay list source: PSA Philippine Standard Geographic Code (62 barangays). Admin MUST verify spelling + completeness against current LGU records before production go-live.
  - Vercel skill packs (next-cache-components, next-forge, nextjs, react-best-practices) continue to false-positive on suffix/import patterns. They are NOT used by this project. Per Rule 28 governance overrides supplemental skill suggestions — keep skipping. Re-evaluate ONLY when doing platform-level changes (proxy.ts migration, Cache Components adoption, framework upgrade).
  - searchForDuplicates name matching is equality-based (case-insensitive). Future enhancement: fuzzy/trigram matching (pg_trgm) or weighted scoring for typo tolerance.
  - "View existing record" link from duplicate gate match cards targets /[tenant]/fisherfolk/[id] — NOW RESOLVES end-to-end after Batch 3a merge. Browser verification still pending — next session should exercise duplicate-search → match → click link → detail page renders.
  - react-signature-canvas v1.1.0-alpha.2 ts-expect-error revisit on lib React-19 update.

# ─── V31 Memory Governance Fields (memory-governance.md §2) ───
TOKEN_ESTIMATE:       ~75K consumed this session (rule-loading floor ~50K + STATE/CHANGELOG/MAP reads ~5K + edits ~10K + git ops + verification ~5K + output ~5K). Within 80K SAFE — squash-merge work fits cleanly when the only file changes are governance docs.
CHECKPOINT_TYPE:      full (Batch 3a code + 3 governance docs updated on main)
FILES_TOUCHED:        [
                        "docs/CHANGELOG_AI.md (Batch 3a entry appended)",
                        "docs/IMPLEMENTATION_MAP.md (Batch 3 section rewritten — 3a checked off, 3b/3c flagged)",
                        ".cline/STATE.md (this rewrite — post-merge state)",
                        "git: squash-merged feat/batch-3a-fisherfolk-detail → main, deleted local + remote branch, pushed main"
                      ]
TIER_CLASSIFICATION:  1 — lightweight (governance + git only, no code changes this session). Code from 12c8b7d preserved unchanged.
SUB_BATCH_SPLIT:      Original Batch 3 split into 3a (detail view — MERGED) + 3b (Vessel Registration — pending) + 3c (Edit Request Workflow — future).
