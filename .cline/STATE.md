# Project State — Fisherfolk Registration and Management System
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-17 PM by CLAUDE_CODE (Phase 8 Batch 2b-2 squash-merged to main; Batch 3 queued for fresh session)

PHASE:        Phase 8 — Iterative Buildout (Batch 2b-2 COMPLETE + MERGED; Batch 3 PENDING in new session)
LAST_DONE:    Squash-merged Batch 2b-2 (photo + signature + QR + Auth/L6-tenant-context fix) into main. Bundled 3 branch commits as one squash on main: e815da4 (2b-2a photo upload via tRPC + MinIO), 9119ca4 (2b-2b SignaturePad + QR code generation), 76891c8 (auth/trpc fix — split Auth.js for Edge runtime + propagate L6 tenant context via runWithTenant in protectedProcedure). Net change: 15 files, +1065/-109. Human verified end-to-end in dev: login as webmaster super_admin → /calapan-city/fisherfolk/register → duplicate search query → multi-step wizard (Personal → Address → Documents → Review) → photo upload, signature capture, QR auto-generation, fisherfolk.create transaction populating qr_code field → all working. Two pre-existing dev bugs surfaced + fixed alongside: (1) middleware imported Auth.js → Prisma → Edge runtime conflict (split into auth/config.ts edge-safe + auth/edge.ts middleware-only + auth/index.ts full Node). (2) tRPC protectedProcedure never called runWithTenant() so every tenant-guarded query threw — now wrapped inline in rate-limiter middleware to preserve enforceAuth's ctx-type narrowing.
NEXT:         Batch 3 — Fisherfolk Detail View + Vessel Registration. START IN NEW SESSION per Rule 24 fresh-context discipline. This session has consumed ~85K context (Batch 2b-2 + dev triage + merge orchestration); above SAFE-zone ceiling. Open new Claude Code session, run "Resume Session" or "Start Phase 8" to continue. Detail page path: /[tenant]/fisherfolk/[id] (currently 404 — Batch 2b-1b.2 view-record link forward-compatible to Batch 3).
BLOCKERS:     none. Long-standing follow-ups carried forward (test infra debt, /platform/tenants UI empty, react-signature-canvas v1.1.0-alpha.2 ts-expect-error revisit on lib React-19 update).
GIT_BRANCH:   main (squash commit pending push to origin at moment of writing; feat/batch-2b-2a-photo-upload pushed to origin + pending local + remote delete).
PORTS:        APP=44387 DB=44377 CACHE=44379 MINIO=44380 PGADMIN=44384 WORKER=44388
MODELS:
  planning:   claude-code (Phase 2 — V31 primary)
  execution:  claude-sonnet-4-6 via Claude Code (V31 primary; Cline deprecated)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)

Phase 8 Batch progress:
  ✅ 1a Shared UI Components — DONE (6 components + barrel index, squash-merged to main 79e79d6)
  ✅ 1b Fisherfolk List Page  — DONE (4 files, squash-merged to main 79e79d6)
  ✅ 2a Fisherfolk Registration Form (basic) — DONE (5 files, merged 58d74fa)
  ⏳ 2b Registration polish — IN PROGRESS:
       ✅ 2b-1 Pickers + Duplicate Search — DONE + MERGED (squash 77efa8c on main, 28 files +2001/-191):
            ✅ 2b-1a Pickers (BarangayPicker + CategoryPicker + wire-in + Checkbox primitive)
            ✅ 2b-1b.1 Backend (Zod schema + fisherfolk.searchForDuplicates query)
            ✅ 2b-1b.2 Frontend gate (duplicate-search-client.tsx + page.tsx wiring + initialValues prop)
            ✅ Memory governance layer (memory-governance.md) + 5 conflicting skill stubs removed
            ✅ *.bak added to .gitignore (excludes framework upgrade snapshots from main)
       ✅ 2b-2  Photo + signature + QR — DONE + MERGED (squash on main, 15 files +1065/-109):
            ✅ 2b-2a Photo upload (upload tRPC router + PhotoUpload + Step 3 Documents wire-in)
            ✅ 2b-2b SignaturePad (react-signature-canvas) + QR code utility + transactional create+update + Documents step signature field + Review rows for photo/signature/QR
            ✅ Auth/middleware/L6 fix bundled in same squash: edge-safe authConfig split, middleware imports edge instance only, protectedProcedure now wraps next() in runWithTenant() so L6 tenant-guard receives context
  ⬜ 3  Fisherfolk Detail View + Vessel Registration — NEXT (start in NEW session)
  ⬜ 3  Fisherfolk Detail View + Vessel Registration — REMAINING
       Note: 2b-1b.2's "View existing record" link points at /[tenant]/fisherfolk/[id] which will 404 until Batch 3 ships the detail page. Acceptable — link is forward-compatible.

Outstanding follow-ups (do not block ongoing batches):
  - TDD test infra missing project-wide (no vitest/jest, zero *.test.*). Schedule "Phase 8 Batch X — test infra setup" before Rule 25 TDD can be strictly enforced. Batch 2b-1b shipped under this acknowledged debt (both halves).
  - Tenant-managed barangay list (other municipalities, auto-seed barangays by municipality, admin edit) — future Settings → Barangays feature. CALAPAN_BARANGAYS is the seed for the City of Calapan FMO tenant only. BarangayPicker is already tenant-agnostic (accepts barangays prop).
  - Calapan barangay list source: PSA Philippine Standard Geographic Code (62 barangays). Admin MUST verify spelling + completeness against current LGU records before production go-live (Rule 29 — list captured at session time, not authoritative).
  - Vercel skill packs (next-cache-components, next-forge, nextjs, react-best-practices) declined load this session AND prior — none apply to feature-level component work. Re-evaluate ONLY when doing platform-level changes (proxy.ts migration, Cache Components adoption, framework upgrade). Same precedent for Clerk auth skill — locked stack is Auth.js v5.
  - searchForDuplicates name matching is equality-based (case-insensitive). Future enhancement: fuzzy/trigram matching (pg_trgm) or weighted scoring for typo tolerance. Current impl catches exact-spelling dupes — sufficient for v1 gate. Capture user feedback during pilot before adding complexity.
  - "View existing record" link from gate match cards targets /[tenant]/fisherfolk/[id] — placeholder, resolves once Batch 3 detail page ships. Encoder workaround until then: copy the ID number, navigate to list, search.

# ─── V31 Memory Governance Fields (memory-governance.md §2) ───
TOKEN_ESTIMATE:       ~85K consumed this session (Batch 2b-2 build + dev triage of auth/L6 bugs + merge orchestration; just above 80K SAFE ceiling but no autocompact thrash observed). Worth noting: ~25K of this was unplanned dev-environment triage (Auth.js Edge split + L6 tenant-context middleware) that exposed pre-existing bugs Batch 2b-1 hadn't exercised end-to-end. Future batches will benefit — login + tenant queries now actually work.
CHECKPOINT_TYPE:      full (Batch 2b-2 squash-merged to main — 15 files in merge)
FILES_TOUCHED:        [
                        "apps/web/package.json",
                        "apps/web/src/middleware.ts",
                        "apps/web/src/server/auth/config.ts",
                        "apps/web/src/server/auth/edge.ts (new)",
                        "apps/web/src/server/auth/index.ts",
                        "apps/web/src/server/trpc/trpc.ts",
                        "apps/web/src/server/trpc/root.ts",
                        "apps/web/src/server/trpc/routers/upload.ts (new)",
                        "apps/web/src/server/trpc/routers/fisherfolk.ts",
                        "apps/web/src/components/fisherfolk/photo-upload.tsx (new)",
                        "apps/web/src/components/fisherfolk/signature-pad.tsx (new)",
                        "apps/web/src/lib/qr-code.ts (new)",
                        "apps/web/src/app/[tenant]/fisherfolk/register/registration-form-client.tsx",
                        "pnpm-lock.yaml",
                        ".cline/STATE.md"
                      ]
TIER_CLASSIFICATION:  2 — moderate (Batch 2b-2 single session, ~9 feature files; pre-existing auth/L6 bugs added ~5 more triage files mid-session for ~14 total — still within Tier 2 envelope thanks to selective reads and reuse of existing infra packages/storage).
SUB_BATCH_SPLIT:      Batch 2b-2 split into 2b-2a (photo) + 2b-2b (signature + QR) per memory-governance §1 Step 2.5. Both shipped on same branch in same session. Plus emergent auth/L6 fix as 3rd commit on same branch. All three squashed as single Batch 2b-2 commit on main.
