# Project State — Fisherfolk Registration and Management System
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-06-26 (PM, Full Auto overnight) by CLAUDE_CODE — Data-Management feature DM-1..4 built + 3,002 FMO records imported + CRITICAL tenant-context bug fixed + real Dashboard UI shipped. Branch feat/data-management (NOT yet merged to main).

PHASE:        Phase 8 — Iterative Buildout. Framework V32.14. THIS SESSION (feat/data-management, 7 commits, NOT merged): built the FMO data-management import pipeline DM-1→DM-4 (133 Vitest tests — FIRST tests in project, partial Rule 25), imported ALL 3,002 production FMO records into dev DB (verified correct at data layer: sex 2085/917, category counts exact, barangay Roman-numeral normalization, 6 null-DOB→Unknown bucket), FIXED a critical pre-existing L6 tenant-guard AsyncLocalStorage bug that silently broke ALL data display app-wide, and shipped a real Dashboard UI rendering the live data. KEY DISCOVERY: dashboard/reports/analytics pages were all UNBUILT STUBS — "verify charts/reports" expanded into BUILD. Dashboard now done; Reports + Analytics + DM-5/6/7 remain.
LAST_DONE:    [2026-06-26 PM overnight] DM-1 normalization engine + Vitest (51b4f50); DM-2 ImportBatch+BarangayAlias schema + dedup/collision (eb2f0a8); DM-3 excel/name parsers + validation report + import tRPC router, Fisherfolk.dateOfBirth+sex made nullable (06958b3); DM-4 asset match/compress/store (023f6a5); canonical category seed fix + import-fmo.ts script (c2fe2e9); FIX tenant-guard ALS pinned to globalThis (bfcfe85 — was breaking every tenant-scoped query: dashboard, lists, notifications); real Dashboard UI + getDemographics query (61db5d6). Import executed via apps/web/scripts/import-fmo.ts against /tmp scratchpad fmo-full-3002.json (stringified SQLite export — booleans MUST be "1"/"0" strings or category mapping silently yields empty categoryIds). Dev tenant calapan-city prepped: 6 canonical categories + cleared test fisherfolk; tenant-admin login created (calapanadmin / Admin123!Calapan) — super_admin can't view tenant pages (no tenantId → guard throws, by design). Dev infra (postgres/minio/valkey on 44377/80/79) brought back up; Next dev server runs on 44387. Browser-verified dashboard via Playwright. Asset linking (--with-assets) NOT yet run (records-first; ~6000 photos/sigs in fmo source public/uploads still to import). Prior session: Batch 3c-2 (Edit Request notifications, PD-003) + Batch 3f (format-agnostic ID, PD-001), built in one parallel wave (3 spec-executors, disjoint files). 3c-2: server/lib/{mailer.ts (nodemailer+tenant SMTP, best-effort), notify.ts (notifyUsers in-app+email+sms via Promise.allSettled, getTenantAdminUserIds), sms.ts (PREPARED/inactive, SMS_ENABLED flag)}; editRequest create→notify admins, approve/reject→notify requester; notification-bell.tsx in header (unread badge poll 30s, popover, mark-read+navigate, mark-all-read). nodemailer added to @frms/web. 3f: registration-form-client idNumber now editable freeform field + Suggest button (was auto-generated FF-YYYY-NNNN only); fisherfolk.create clearer dup message; idNumber already freeform per-tenant-unique String (no schema/regex change). Both committed on branch feat/batch-3c2-3f → ff-merged main (0a7a403, b6572cd) → pushed. Verified each + consolidated: apps/web tsc EXIT=0, next lint clean, shared tsc EXIT=0. Prior this session: Batch 3c-1 — core Edit Request workflow (Full Auto). PRODUCT.md flow #3 on locked decisions PD-002/004. 10 files (6 new + 4 mod) via 4 parallel spec-executors. Router: editRequest.create validates fieldChanges keys ⊆ fisherfolkUpdateSchema (24 keys, rejects unknown/empty) + stores changed-keys-only (=history per PD-002); editRequest.history(fisherfolkId) added; fisherfolk.completeRecord encoderProcedure = PD-004 bypass (fills currently-EMPTY fields directly, FORBIDDEN if any targeted field already populated). UI: fisherfolk/[id]/edit form (prefilled all editable fields, diff-routes fill-empty→completeRecord instant / else→editRequest.create approval, info banner) + Edit button on detail; edit-requests admin queue (default PENDING) + columns; edit-requests/[id] diff viewer (old red strikethrough→new green) + Approve/Reject-with-reason dialog + rejection-history (PD-004). Sidebar nav item already scaffolded. Verified apps/web tsc EXIT=0, next lint clean, shared tsc EXIT=0. feat/batch-3c1-edit-request → squash d73a77c → pushed → branch deleted. Prior this session: locked owner answers PD-001/002/003/004 (d458903). Earlier: Batch 3d — signed-URL role fix (Full Auto): upload.getDownloadUrl encoderProcedure → protectedProcedure so Viewer + Bantay Dagat can fetch media signed URLs (PRODUCT.md line 268 requires Bantay Dagat to see photos for field verification); cross-tenant still blocked in getFileDownloadUrl. uploadFile write path unchanged. tsc+lint clean. fix/batch-3d-signed-url-role → squash 28ca431 → pushed → branch deleted. Resolves the long-standing encoderProcedure photo-visibility follow-up for both fisherfolk + vessel detail. Prior this session: Batch 3b — Vessel Registration (Full Auto). Built the full vessel feature mirroring fisherfolk: list page + columns + list client (/[tenant]/vessels — MFVR linked to detail, ACTIVE/IMPOUNDED/INACTIVE status filter, search); read-only detail (/[tenant]/vessels/[id] — all MFVR fields grouped in cards, linked owners → fisherfolk profiles, active violations panel, QR render via renderQRDataUrl, role-gated photo); single-page register form (/[tenant]/vessels/register — all fields, fishing-gear chips, optional fisherfolk owner picker via fisherfolk.list search, optional photo). Hardened vessel.ts router to spec: create auto-generates+stores QR (buildQRPayload regNo=mfvrNumber, in a transaction mirroring fisherfolk); ownerIds OPTIONAL (default [], owners.connect omitted when empty) per PRODUCT.md "optionally link owner" flow #10; clearer duplicate-MFVR CONFLICT message; create accepts optional vesselPhoto. Shared: upload ENTITY_TYPES += "vessel-photo"; PhotoUpload given optional entityType prop (default fisherfolk-photo, 100% backward-compatible). 10 files (6 new + 4 modified). Orchestrated as Opus architect + 6 spec-executor (Sonnet) dispatches + 2 Explore scouts (lean context). Verified: apps/web tsc --noEmit EXIT=0, next lint clean across whole web src (NO fisherfolk regression from shared edits), packages/shared tsc EXIT=0. Governance docs updated (CHANGELOG_AI Batch 3b entry, IMPLEMENTATION_MAP Batch 3 section + Phase row). feat/batch-3b-vessel-registration → squash-merged main (c52a1ab) → pushed → branch deleted. Prior: Adopted data-management patterns from the production FMO reporting tool (fmo.powerbyte.app, 3,003 records) into docs/PRODUCT.md (SPEC-ONLY): new "Data Management & Normalization Standards" subsection (6-activity category taxonomy + free-text→flag mapping; DOB/sex/barangay/contact normalization rules; dedup/idempotency; ID-collision integrity; asset linking + missing-asset CSV; incremental import; legacy-ID preservation) + seeded 6 default categories. Created docs/PENDING_DECISIONS.md PD-001 (new-reg ID format MR-CL-NNNNNN-YYYY vs FF-YYYY-NNNN — deferred to FMO, non-blocking). Analyzed via 3 parallel Explore agents (lean context). Phase-7 propagation (seed + import service) deferred. Prior: AIEF V31→V32.14 upgrade (Full Auto, owner-approved): sync-to-project.sh + deploy.sh applied; CLAUDE.md now compact V32.14, .ai_prompt/ holds 7 detail files load-on-demand, .claude/rules/ emptied, spec-executor agent + scripts/lint-deploy.sh + scripts/design-stop-hook.sh installed; settings.json MERGED (custom anti-thrashing hook 7bf35bf PRESERVED — jq-verified); 15 stale v31 files removed; new gov gates Rule 33 + .ai_prompt/privacy.md (PH Data Privacy + WCAG 2.2 AA). Governance docs updated (CHANGELOG, MAP, project.memory V31→V32.14). Committed on chore/aief-v32-upgrade → squash-merge main → pushed. Prior: Governance Sync (2026-05/06-25): added CHANGELOG_AI.md reconciliation entries for Batch 2b-1 (77efa8c) + Batch 2b-2 (9ab5039); updated IMPLEMENTATION_MAP.md (Phase 8 row, Batch 2b section marked complete, decision count 12→16); cleared the resolved drift follow-up. Prior: Squash-merged Batch 3a (Fisherfolk read-only detail view + list nav) to main. Collapsed 2 feature-branch commits (12c8b7d code + 2b32d90 STATE.md update) into one squash commit on main. Pushed main to origin. Deleted feat/batch-3a-fisherfolk-detail local + remote. CHANGELOG_AI.md + IMPLEMENTATION_MAP.md updated for Batch 3a. New route /[tenant]/fisherfolk/[id] now live: server page.tsx + fisherfolk-detail-client.tsx render profile fields + photo (signed URL via upload.getDownloadUrl) + signature (same) + QR (renderQRDataUrl → PNG data URL). List page columns.tsx idNumber column wraps in <Link> via IdNumberCell sub-component (uses useParams for tenant slug). Reused existing fisherfolk.getById — no router changes. End-to-end flow now resolves: register → duplicate-search "View existing record" → detail page renders (was 404 before).
NEXT:         [OVERNIGHT CONTINUATION — data-management feature] Branch feat/data-management holds DM-1..4 + import + tenant-fix + dashboard, NOT yet merged to main (verify + squash-merge when owner approves). REMAINING to fully satisfy "charts AND reports displaying with content": (1) REPORTS page — currently a 10-line STUB, no reports router; PRODUCT.md flow #13 = 9 report types (Member List, New Reg, Renewed, Inactive, Senior Citizens, Voter-Eligible, Violations, Vessels, Family Clusters) + filters + PDF/Excel export (exceljs already a dep; PDF needs a lib). BIG batch. (2) ANALYTICS page — 10-line STUB; PRODUCT.md flow #12 rich analytics (gender, senior, voter, category, barangay density map, Charts↔Maps toggle, trends, birthdays). Consider adding recharts for real charts (dashboard currently uses CSS bars, no chart lib). (3) DM-5 import WIZARD UI (5-step, /[tenant]/import — backend router import.ts already built + verified). (4) DM-6 incremental import mode. (5) DM-7 wire normalization into manual register/renew save + tenant Barangay typo-map Settings tab. (6) Run asset import (--with-assets) to populate photos/signatures. (7) PRODUCT.md back-port of PD-001..004 still owed (Rule 1). NOTE the notification bell 500s were the SAME tenant-guard bug — now fixed. ALSO: super_admin landing can't open tenant pages (expected) — document or add a tenant-impersonation path if owner wants super_admin dashboards. ───────── PRIOR (still valid background): Edit Request feature is COMPLETE — queue from the owner's PD answers is fully drained. No un-gated batch is currently queued; next unit is the OWNER'S CALL. Candidate next units (surface to owner): (A) Phase 6 — Docker + Visual QA / browser verification pass: NOTHING built since Batch 2b-2 has had a live browser exercise (Rule 16) — vessels, edit-request flow, notifications, mixed-ID registration all need a real run; this is the highest-value next step to convert "tsc/lint clean" into "verified working". (B) TDD test-infra setup (zero tests project-wide, Rule 25 blocker). (C) V32.9 privacy/Rule 33 compliance pass (PH Data Privacy + WCAG 2.2 AA gov gate — FRMS is LGU gov w/ PII; read .ai_prompt/privacy.md; start read-only audit). (D) Phase-7 propagation of the FMO data-management spec (seed 6 categories, import-normalization service, data-quality CSV, incremental import). (E) remaining Violation/Bantay-Dagat flows, ID card generation, dashboard KPIs per PRODUCT.md page inventory. Pending owner items: PD-004 is a provisional agent-default (owner may flip bypass scope / resubmit model); SMS channel is prepared-but-off (flip SMS_ENABLED + implement a sender to activate). PRODUCT.md back-port of PD-001/002/003/004 still owed (Rule 1 — human edits PRODUCT.md). Fresh-context per Rule 24 for the next batch. Notification Prisma model (schema.prisma ~L459) + NotificationType enum (~L73) + notification.ts router already exist (in-app substrate). Build: (1) email mailer util using tenant SMTP settings (tenant schema has smtp fields — no nodemailer in app yet, add it); (2) wire editRequest.create → notify all tenant admins (in-app + email), approve/reject → notify the requester (in-app + email) with reason; (3) SMS = define a channel interface + no-op sender stub + config flag (inactive). Check for an existing in-app notification-center UI; add a bell/dropdown if absent. Scope ~6-9 files; split if >12. THEN Batch 3f — ID-format flexibility (PD-001 locked): fisherfolk.create accepts manual freeform idNumber (any format) + per-tenant uniqueness check; registration form idNumber becomes manual entry with generateNextIdNumber as optional "suggest"; drop any format regex; legacy IDs already preserved. ~3-4 files. Fresh-context per Rule 24. ORIGINAL NEXT (still valid context): Edit Request backend was already scaffolded. IMPORTANT scouting finding: the backend is ALREADY scaffolded — EditRequest Prisma model + enum exist (schema.prisma ~line 346) AND editRequest.ts router (list/getById/create/approve/reject, 216 LoC) is implemented. Remaining work is UI + notifications, and its core hinges on [WHAT] answers: PD-002 (editable-field whitelist — blocks encoder Edit form), PD-003 (notification channel — blocks notify triggers), PD-004 (approval-bypass scope + resubmit/history). The ONLY un-gated slice is the admin-side review UI (approval queue using list({status:PENDING}) + a field-agnostic diff viewer + approve/reject) — but it's a half-feature without the gated encoder Edit form, so it was NOT built speculatively (avoid rework once PD-002 is answered). When owner answers PD-002/003: back-port to PRODUCT.md + DECISIONS_LOG, then build 3c UI (est. ~6-9 files; split encoder-form / admin-queue if >12). OTHER un-gated future batches (owner-awareness recommended before starting — new directions): TDD test-infra setup (Rule 25 prerequisite, currently zero tests); V32.9 privacy/Rule 33 compliance pass (PH Data Privacy + WCAG 2.2 AA gov gate — FRMS is LGU gov w/ PII; read .ai_prompt/privacy.md — start with a read-only audit/report). Fresh-context discipline per Rule 24: open a new session for the next batch.
BLOCKERS:     none. Long-standing follow-ups carried forward. PD-001 (registration ID format) still deferred to FMO/owner — non-blocking.
SECURITY:     ✅ Addressed 2026-06-26 (background security review, MEDIUM): notify.ts email HTML built `<p>${message}</p>` unescaped while message carries user-controlled text (fisherfolk names, rejection reasons) → stored XSS in email client. Fixed with escapeHtml() before \n→<br> (commit 37417d4, logged 009547b). Plain-text channels unaffected. FORWARD NOTE: notify.ts is the shared notification path — any future caller building richer HTML email must pass already-escaped/templated body.
GIT_BRANCH:   feat/data-management @ 61db5d6 — working tree CLEAN, NOT pushed, NOT merged to main (main still @ fa5c5a8). 7 commits this session: DM-1 51b4f50, DM-2 eb2f0a8, DM-3 06958b3, DM-4 023f6a5, seed+script c2fe2e9, tenant-guard fix bfcfe85, dashboard 61db5d6 (+gitignore). Squash-merge to main pending owner review (the tenant-guard fix in particular is a high-value standalone fix worth landing). PRIOR main state was: main @ 009547b — pushed to origin, working tree CLEAN, 0 ahead/0 behind. All feature branches deleted. This session (since bd7f57f): 3b vessel (c52a1ab) + 3d signed-url (28ca431) + decision locks (d458903) + 3c-1 edit-request (d73a77c) + 3c-2 notifications (0a7a403) + 3f ID-flex (b6572cd) + XSS fix (37417d4) + governance, all squash/ff-merged + pushed.
PORTS:        APP=44387 DB=44377 CACHE=44379 MINIO=44380 PGADMIN=44384 WORKER=44388
MODELS:
  planning:   claude-code (Phase 2 — V31 primary)
  execution:  claude-sonnet-4-6 via Claude Code (V31 primary; Cline deprecated). This session ran as Opus 4.7 per environment override — flag for future cost review if pattern continues.
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)

Phase 8 Batch progress:
  ✅ 1a Shared UI Components — DONE (6 components + barrel index, squash-merged to main 79e79d6)
  ✅ 1b Fisherfolk List Page  — DONE (4 files, squash-merged to main 79e79d6)
  ✅ 2a Fisherfolk Registration Form (basic) — DONE (5 files, merged 58d74fa)
  ✅ 2b Registration polish — DONE + MERGED:
       ✅ 2b-1 Pickers + Duplicate Search — squash 77efa8c on main (28 files +2001/-191)
       ✅ 2b-2 Photo + signature + QR + Auth/L6 fix — squash on main (15 files +1065/-109, commit 9ab5039)
  ⏳ 3  Fisherfolk Detail View + Vessel Registration — IN PROGRESS:
       ✅ 3a Fisherfolk Detail View (read-only) — SQUASH-MERGED to main 2026-05-17 PM (3 files +267/-1)
       ✅ 3b Vessel Registration — SQUASH-MERGED to main 2026-06-25 (c52a1ab, 10 files: 6 new + 4 mod). List + detail + register form + router QR/owners-optional/photo + shared upload/PhotoUpload reuse. tsc+lint clean.
       ⬜ 3c Edit Request Workflow — NEXT (split out of original Batch 3 scope; admin queue + diff viewer + notifications)

AIEF ALIGNMENT CHECK (2026-06-25 — register-to-aief skill):
  - FRMS confirmed framework app; stack FULLY ALIGNED with locked stack, 0 true violations
    (two sub-agent false positives rejected: ioredis is the Valkey CLIENT driver not a Rule-14
    violation — server is valkey/valkey:7-alpine; "147 test files" counted node_modules,
    git-tracked tests = 0 = known documented debt). Fleet registry updated: FRMS declared V31.
    Framework lesson recorded in AIEF LESSONS_REGISTRY.md (client-vs-server + git-ls-files test count).
  - VERSION GAP: app V31 → framework source V32.14 (14 minor behind). STALE.
  - prep-sync DRY-RUN run (read-only, nothing written): would copy 25-file V32.14 deliverable set
    → 24 into .ai_prompt/ + deploy.sh at root. NEW files vs current V31 set: privacy.md (V32.9
    PH Data-Privacy/NPC + WCAG 2.2 AA gov HARD GATE — TOP PRIORITY, FRMS is LGU gov w/ PII),
    motion.md, design-principles.md, spec-executor.md, lint-deploy.sh, design-stop-hook.sh,
    AI_Tools_Reference.md, Security_Checklist.md.
  - SCHEDULED: V32 upgrade as its OWN dedicated session (NOT interleaved with Batch 3b). Steps:
    (1) bash $AIEF/sync-to-project.sh <FRMS>  (copies .ai_prompt/ + deploy.sh)
    (2) bash deploy.sh  (relayout: rule files → .ai_prompt/, .claude/rules/ emptied, compact
        CLAUDE.md written, spec-executor agent installed; settings.json is CREATE-OR-MERGE —
        VERIFY the custom anti-thrashing UserPromptSubmit hook 7bf35bf survives the merge)
    (3) restart Claude Code; run memory-governance.md §5 mid-project adoption baseline checkpoint.
    AIEF=/home/me/UbuntuDevFiles/1_COMPANY_DEV/Powerbyte-AIEF. HARD HOLD respected — not yet applied.

Outstanding follow-ups (do not block ongoing batches):
  - TDD test infra missing project-wide (no vitest/jest, zero *.test.*). Schedule "Phase 8 Batch X — test infra setup" before Rule 25 TDD can be strictly enforced. Batch 3a shipped under this acknowledged debt.
  - ✅ RESOLVED 2026-06-25 (Batch 3d, squash 28ca431): upload.getDownloadUrl was encoderProcedure → Viewer + Bantay Dagat saw "No photo/No signature" placeholders on fisherfolk + vessel detail. Chose fix option (b): getDownloadUrl → protectedProcedure. Safe because getFileDownloadUrl already enforces tenant isolation (extractTenantFromKey(key) !== ctx.tenantId → "Access denied", storage/upload.ts:68-71). uploadFile (write) stays encoderProcedure. Now all authenticated same-tenant roles can fetch media signed URLs per PRODUCT.md roles table.
  - ✅ RESOLVED 2026-06-25 (Governance Sync): IMPLEMENTATION_MAP.md Phase row + Batch 2b section now reflect 2b-1/2b-2/3a merged; decision count corrected 12→16; CHANGELOG_AI.md gained reconciliation entries for Batch 2b-1 (77efa8c) and Batch 2b-2 (9ab5039).
  - Tenant-managed barangay list (other municipalities, auto-seed barangays by municipality, admin edit) — future Settings → Barangays feature.
  - Calapan barangay list source: PSA Philippine Standard Geographic Code (62 barangays). Admin MUST verify spelling + completeness against current LGU records before production go-live.
  - Vercel skill packs (next-cache-components, next-forge, nextjs, react-best-practices) continue to false-positive on suffix/import patterns. They are NOT used by this project. Per Rule 28 governance overrides supplemental skill suggestions — keep skipping. Re-evaluate ONLY when doing platform-level changes (proxy.ts migration, Cache Components adoption, framework upgrade).
  - searchForDuplicates name matching is equality-based (case-insensitive). Future enhancement: fuzzy/trigram matching (pg_trgm) or weighted scoring for typo tolerance.
  - "View existing record" link from duplicate gate match cards targets /[tenant]/fisherfolk/[id] — NOW RESOLVES end-to-end after Batch 3a merge. Browser verification still pending — next session should exercise duplicate-search → match → click link → detail page renders.
  - react-signature-canvas v1.1.0-alpha.2 ts-expect-error revisit on lib React-19 update.

# ─── V31 Memory Governance Fields (memory-governance.md §2) ───
TOKEN_ESTIMATE:       Architect (Opus) main context kept lean by dispatch — heavy code work (10 files) executed in 7 spec-executor (Sonnet) subagents + 2 Explore scouts, each isolated. Main-loop input stayed well within SAFE: scouting briefs + 3 small router reads + governance edits. Dispatch ratio sonnet_writes/opus_writes ≈ 8/0 code writes (Opus wrote only governance docs on allow-list + 1 trivial dead-import edit) → ≥3.0 target met (R9).
CHECKPOINT_TYPE:      full (Batch 3b: 10 code files merged to main + 3 governance docs updated)
FILES_TOUCHED:        [
                        "apps/web/src/app/[tenant]/vessels/{page.tsx(mod), vessels-list-client.tsx, columns.tsx, [id]/page.tsx, [id]/vessel-detail-client.tsx, register/page.tsx, register/registration-form-client.tsx}",
                        "apps/web/src/server/trpc/routers/{vessel.ts(QR+owners-optional+photo+dup-msg), upload.ts(vessel-photo enum)}",
                        "apps/web/src/components/fisherfolk/photo-upload.tsx (optional entityType prop)",
                        "docs/CHANGELOG_AI.md (Batch 3b entry prepended)",
                        "docs/IMPLEMENTATION_MAP.md (Batch 3 section + Phase row updated — 3b checked off, 3c next)",
                        ".cline/STATE.md (this rewrite — post-3b-merge state)",
                        "git: feat/batch-3b-vessel-registration → squash-merge main c52a1ab → pushed → branch deleted"
                      ]
TIER_CLASSIFICATION:  3 — feature batch (new code surface, multi-agent dispatch, verified tsc+lint, squash-merge+push).
SUB_BATCH_SPLIT:      Original Batch 3 split into 3a (detail view — MERGED) + 3b (Vessel Registration — MERGED c52a1ab) + 3c (Edit Request Workflow — next).
