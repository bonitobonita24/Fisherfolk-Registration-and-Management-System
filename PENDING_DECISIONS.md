# Pending Owner Decisions

Owner `[WHAT]` calls the autonomous loop must NOT decide itself. The loop keeps
rebooting and re-surfacing these until the owner answers. Technical `[HOW]` is the
conductor's to decide and never lands here.

## Open decisions

- [ ] **Merge / push the UI rehab branch?** `feat/ui-rehab-pro` (8 rehab commits, 42 ahead of
  origin/main) is finished and verified but UNMERGED + UNPUSHED per the standing HARD HOLD —
  merging to `main` auto-deploys staging via CI. Owner decides when to: (a) push the branch +
  open a stacked PR, (b) merge the whole stack (PRs #1→#4 then this), or (c) keep holding.
  Until answered, the loop holds (no push/merge).

## Resolved

- [x] 2026-06-29 — Palette direction → **modern dark, orange + navy** (owner picked after a light
  "Deep Sea Teal" trial); greenish chart accent retuned to navy. Shipped in the rehab.
